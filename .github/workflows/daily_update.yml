name: Daily Garmin Sync

on:
  schedule:
    - cron: '0 6 * * *'  # Dagelijks om 04:00 UTC
  workflow_dispatch:      # Manueel triggeren via GitHub UI

jobs:
  run-scripts:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3

      - name: 🔧 Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: ▶️ Run Garmin extraction script
        run: python Garmin_connect_extraction.py
        env:
          GARMIN_EMAIL: ${{ secrets.GARMIN_EMAIL }}
          GARMIN_PASSWORD: ${{ secrets.GARMIN_PASSWORD }}

      - name: ▶️ Run Garmin health script
        run: python Garmin_connect_health.py
        env:
          GARMIN_EMAIL: ${{ secrets.GARMIN_EMAIL }}
          GARMIN_PASSWORD: ${{ secrets.GARMIN_PASSWORD }}

      - name: 📝 Log time of successful update
        run: |
          echo "Laatste update: $(date -u)" >> update_log.txt

      - name: 💾 Commit updated CSVs + log
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add *.csv update_log.txt || echo "Niets om te committen"
          git diff --cached --quiet || git commit -m "🔄 Dagelijkse Garmin sync + log"
          git push https://x-access-token:${{ secrets.GH_PAT }}@github.com/${{ github.repository }}.git HEAD:main
        continue-on-error: true

